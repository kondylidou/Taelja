-- Generated by taelja2lean.py
-- Lean 4 verification of Taelja proof output

namespace VampireTestHavehenceInEqchain

-- Uninterpreted sort
axiom α : Type

-- Constants
axiom a : α

axiom f : α → α
axiom g : α → α
axiom h : α → α
axiom s : α → α

axiom p : α → Prop
axiom q : α → Prop

-- Axiom 1
axiom ax1 : p a
-- Axiom 2
axiom ax2 : ∀ (x : α), p x → (f x) = (g x)
-- Axiom 3
axiom ax3 : q a
-- Axiom 4
axiom ax4 : ∀ (x : α), q x → (g x) = (h x)

-- Lemma 5
theorem taelja_lemma5 : (f a) = (g a) := by
  have h1 : p a := by apply ax1
  have h2 : (f a) = (g a) := by apply ax2 <;> first | assumption | simp_all
  exact h2

-- Lemma 6
theorem taelja_lemma6 : (g a) = (h a) := by
  have h1 : q a := by apply ax3
  have h2 : (g a) = (h a) := by apply ax4 <;> first | assumption | simp_all
  exact h2

-- Goal 1
theorem taelja_goal1 : (s (f a)) = (s (h a)) := by
  calc s (f a) = s (g a) := by rw [taelja_lemma5]
      _ = s (h a) := by rw [taelja_lemma6]

end VampireTestHavehenceInEqchain

