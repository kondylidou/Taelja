-- Generated by taelja2lean.py
-- Lean 4 verification of Taelja proof output

namespace VampireTestEqchainInHavehence

-- Uninterpreted sort
axiom α : Type

-- Constants
axiom a : α

axiom f : α → α
axiom g : α → α
axiom h : α → α

axiom p : α → Prop

-- Axiom 1
axiom ax1 : ∀ (x : α), (f x) = (g x)
-- Axiom 2
axiom ax2 : ∀ (x : α), (g x) = (h x)
-- Axiom 3
axiom ax3 : ∀ (x : α), (f x) = (h x) → p x

-- Lemma 4
theorem taelja_lemma4 : ∀ (x : α), (f x) = (h x) := by
  intro x
  calc f x = g x := by rw [ax1]
      _ = h x := by rw [ax2]

-- Goal 1
theorem taelja_goal1 : p a := by
  have h1 : (f a) = (h a) := by apply taelja_lemma4
  have h2 : p a := by apply ax3 <;> first | assumption | simp_all
  exact h2

end VampireTestEqchainInHavehence

