-- Generated by taelja2lean.py
-- Lean 4 verification of Taelja proof output

namespace VampireTestPrelemmatizeSymEq

-- Uninterpreted sort
axiom α : Type

-- Constants
axiom a : α

axiom h : α → α
axiom k : α → α

axiom p : α → Prop
axiom q : α → Prop
axiom r : α → Prop

-- Axiom 1
axiom ax1 : (h a) = (k a)
-- Axiom 2
axiom ax2 : ∀ (x : α), (h x) = (k x) → p x
-- Axiom 3
axiom ax3 : ∀ (x : α), (k x) = (h x) → q x
-- Axiom 4
axiom ax4 : ∀ (x : α), p x → q x → r x

-- Lemma 5
theorem taelja_lemma5 : q a := by
  have h1 : (h a) = (k a) := by apply ax1
  have h2 : q a := by apply ax3 <;> first | assumption | simp_all
  exact h2

-- Goal 1
theorem taelja_goal1 : r a := by
  have h1 : (h a) = (k a) := by apply ax1
  have h2 : p a := by apply ax2 <;> first | assumption | simp_all
  have h3 : q a := by apply taelja_lemma5
  have h4 : r a := by apply ax4 <;> first | assumption | simp_all
  exact h4

end VampireTestPrelemmatizeSymEq

