-- Generated by taelja2lean.py
-- Lean 4 verification of Taelja proof output

namespace VampireHornExampleEqHeadInlined

-- Uninterpreted sort
axiom α : Type

-- Constants
axiom a : α
axiom zero : α

axiom f : α → α
axiom g : α → α

axiom p : α → Prop
axiom q : α → Prop

-- Axiom 1
axiom ax1 : ∀ (x : α), (f x) = (g x)
-- Axiom 2
axiom ax2 : ∀ (x : α), p (f x)
-- Axiom 3
axiom ax3 : ∀ (x : α), q (f x)
-- Axiom 4
axiom ax4 : ∀ (x : α), p x → q x → x = zero

-- Goal 1
theorem taelja_goal1 : (g a) = zero := by
  have h1 : p (f a) := by apply ax2
  have h2 : q (f a) := by apply ax3
  have h3 : (f a) = zero := by apply ax4 <;> first | assumption | simp_all
  have h4 : (g a) = zero := by have h_rw := h3; rw [ax1] at h_rw; exact h_rw
  exact h4

end VampireHornExampleEqHeadInlined

