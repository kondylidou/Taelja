-- Generated by taelja2lean.py
-- Lean 4 verification of Taelja proof output

namespace VampirePaperHeu

-- Uninterpreted sort
axiom α : Type

-- Constants
axiom a : α

axiom f : α → α
axiom g : α → α

axiom p : α → Prop

-- Axiom 1
axiom ax1 : ∀ (x : α), (f x) = x
-- Axiom 2
axiom ax2 : ∀ (x : α), (g x) = (f x)
-- Axiom 3
axiom ax3 : (g a) = a → p (g a)

-- Lemma 4
theorem taelja_lemma4 : ∀ (x : α), (g x) = x := by
  intro x
  calc g x = f x := by have h_rw := ax2 x; rw [h_rw]
      _ = x := by have h_rw := ax1 x; rw [h_rw]

-- Goal 1
theorem taelja_goal1 : p (g a) := by
  have h1 : (g a) = a := by apply taelja_lemma4
  have h2 : p (g a) := by apply ax3 <;> (first | assumption | exact Eq.symm (by assumption))
  exact h2

end VampirePaperHeu

