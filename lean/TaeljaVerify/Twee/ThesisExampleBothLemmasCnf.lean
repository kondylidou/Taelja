-- Generated by taelja2lean.py
-- Lean 4 verification of Taelja proof output

namespace TweeThesisExampleBothLemmasCnf

-- Uninterpreted sort
axiom α : Type

-- Constants
axiom a : α

axiom f : α → α
axiom g : α → α

axiom p : α → Prop
axiom q : α → Prop
axiom s : α → Prop

-- Axiom 1
axiom ax1 : s a
-- Axiom 2
axiom ax2 : ∀ (x : α), (g x) = (f x)
-- Axiom 3
axiom ax3 : ∀ (y : α), (f y) = y
-- Axiom 4
axiom ax4 : ∀ (y : α), (g y) = y → q y
-- Axiom 5
axiom ax5 : ∀ (y : α), s y → q y → p y

-- Lemma 6
theorem taelja_lemma6 : ∀ (x : α), (g x) = x := by
  intro x
  calc g x = f x := by have h_rw := ax2 x; rw [h_rw]
      _ = x := by have h_rw := ax3 x; rw [h_rw]

-- Lemma 7
theorem taelja_lemma7 : q a := by
  have h1 : (g a) = a := by apply taelja_lemma6
  have h2 : q a := by apply ax4 <;> (first | assumption | exact Eq.symm (by assumption))
  exact h2

-- Goal 1
theorem taelja_goal1 : p a := by
  have h1 : s a := by apply ax1
  have h2 : q a := by apply taelja_lemma7
  have h3 : p a := by apply ax5 <;> (first | assumption | exact Eq.symm (by assumption))
  exact h3

end TweeThesisExampleBothLemmasCnf

