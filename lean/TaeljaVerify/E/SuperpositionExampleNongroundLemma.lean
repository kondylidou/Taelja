-- Generated by taelja2lean.py
-- Lean 4 verification of Taelja proof output

namespace ESuperpositionExampleNongroundLemma

-- Uninterpreted sort
axiom α : Type

-- Constants
axiom a : α

axiom f : α → α
axiom g : α → α

axiom p : α → Prop

-- Axiom 1
axiom ax1 : ∀ (x : α), (g x) = (f x)
-- Axiom 2
axiom ax2 : ∀ (x : α), (f x) = x
-- Axiom 3
axiom ax3 : ∀ (x : α), (g x) = x → p (g x)

-- Lemma 4
theorem taelja_lemma4 : ∀ (x : α), (g x) = x := by
  intro x
  calc g x = f x := by rw [ax1]
      _ = x := by rw [ax2]

-- Goal 1
theorem taelja_goal1 : p (g a) := by
  have h1 : (g a) = a := by apply taelja_lemma4
  have h2 : p (g a) := by apply ax3 <;> first | assumption | simp_all
  exact h2

end ESuperpositionExampleNongroundLemma

