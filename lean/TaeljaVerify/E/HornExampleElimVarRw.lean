-- Generated by taelja2lean.py
-- Lean 4 verification of Taelja proof output

namespace EHornExampleElimVarRw

-- Uninterpreted sort
axiom α : Type

-- Constants
axiom a : α
axiom c : α

axiom f : α → α
axiom g : α → α

axiom q : α → α → Prop

-- Axiom 1
axiom ax1 : ∀ (x : α), q a x
-- Axiom 2
axiom ax2 : ∀ (x : α), (g x) = c
-- Axiom 3
axiom ax3 : ∀ (x : α) (y : α), q x y → (f x) = (g y)

-- Goal 1
theorem taelja_goal1 : (f a) = c := by
  have h1 : ∀ (x : α), q a x := fun x => by apply ax1
  have h2 : ∀ (x : α), (f a) = (g x) := fun x => by have h1_i := h1 x; apply ax3 <;> assumption
  have h3 : (f a) = c := by have h_rw := h2; simp only [ax2] at h_rw; exact h_rw a
  exact h3

end EHornExampleElimVarRw

