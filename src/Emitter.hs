module Emitter where

import Types

-- | Render a structured proof to the output format described in
--   horn_approach.md §2 and §4.
emit :: StructuredProof -> String
emit = error "Emitter.emit: not yet implemented"
